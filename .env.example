# Copy this file to .env and fill in values as needed.
# All fields are optional – the dashboard works without any keys.

# Path to the SQLite database (default: polymarket.db in the working directory)
# DB_PATH=polymarket.db

# (Reserved for future use – e.g. a private CLOB API key for higher rate limits)
# CLOB_API_KEY=
